version: "3.9"
name: progetto-flutter
services:
  flutter:
    image: ghcr.io/cirruslabs/flutter:3.24.0
    container_name: flutter-dev
    working_dir: /workspace
    volumes:
      - ./:/workspace
    ports:
      - "${WEB_PORT:-8090}:8090"
      - "${DEVTOOLS_PORT:-9090}:9090"
    environment:
      TZ: ${TZ:-Europe/Rome}
    command: bash -lc "flutter pub get && flutter run -d web-server --web-port=8090 --web-hostname=0.0.0.0"
    stdin_open: true
    tty: true
    restart: unless-stopped

  laravel-backend:
    image: composer:latest
    container_name: laravel-backend
    working_dir: /var/www/html
    volumes:
      - ./backend_app:/var/www/html
    command: ["php", "-S", "0.0.0.0:8000", "-t", "public"]
    ports:
      - "8000:8000"
    depends_on:
      - db
      - mongo
    environment:
      COMPOSER_ALLOW_SUPERUSER: 1

  php:
    build:
      context: .
      dockerfile: Dockerfile.php
    image: progetto-flutter-php:8.2
    container_name: php-backend
    ports:
      - "${PHP_PORT:-8084}:80"
    volumes:
      - ./backend-php:/var/www/html
      - ./apache/vhost.conf:/etc/apache2/sites-available/000-default.conf
    environment:
      TZ: ${TZ:-Europe/Rome}
      DB_HOST: db
      DB_NAME: ${MYSQL_DATABASE:-appdb}
      DB_USER: ${MYSQL_USER:-app}
      DB_PASS: ${MYSQL_PASSWORD:-changemeApp!}
    depends_on:
      - db
    restart: unless-stopped

  db:
    image: mysql:8.0.39
    container_name: db
    ports:
      - "${MYSQL_HOST_PORT:-3309}:3306"
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:?set in .env}
      MYSQL_DATABASE: ${MYSQL_DATABASE:-appdb}
      MYSQL_USER: ${MYSQL_USER:-app}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:?set in .env}
      TZ: ${TZ:-Europe/Rome}
    volumes:
      - mysql_data:/var/lib/mysql
    restart: unless-stopped

  phpmyadmin:
    image: phpmyadmin:5.2.1
    container_name: pma
    environment:
      PMA_HOST: db
      PMA_USER: root
      PMA_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      TZ: ${TZ:-Europe/Rome}
    ports:
      - "${PMA_PORT:-8083}:80"
    depends_on:
      - db
    restart: unless-stopped

  mongo:
    image: mongo:7.0
    container_name: mongo-db
    ports:
      - "${MONGO_HOST_PORT:-27018}:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: ${MONGO_INITDB_ROOT_USERNAME}
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_INITDB_ROOT_PASSWORD}
    volumes:
      - mongo_data:/data/db
    restart: unless-stopped

  mongo-express:
    image: mongo-express:1.0.2-20
    container_name: mongo-express
    environment:
      ME_CONFIG_MONGODB_SERVER: mongo
      ME_CONFIG_MONGODB_ADMINUSERNAME: ${MONGO_INITDB_ROOT_USERNAME}
      ME_CONFIG_MONGODB_ADMINPASSWORD: ${MONGO_INITDB_ROOT_PASSWORD}
      ME_CONFIG_BASICAUTH_USERNAME: ${MONGO_INITDB_ROOT_USERNAME}
      ME_CONFIG_BASICAUTH_PASSWORD: ${MONGO_INITDB_ROOT_PASSWORD}
      TZ: ${TZ:-Europe/Rome}
    ports:
      - "${MONGO_EXPRESS_PORT:-8085}:8081"
    depends_on:
      - mongo
    restart: unless-stopped

volumes:
  mysql_data:
  mongo_data: